import Typography from '~/components/atom/typography';
import { Card, CardHeader } from '~/components/ui/card';

const MembersTable = () => (
    <Card className="flex-grow">
        <CardHeader>
            <Typography variant="h4">Members Table</Typography>
        </CardHeader>
    </Card>
);

export default MembersTable;
